import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.busmaintenance.pro',
  appName: 'Bus Maintenance Pro',
  webDir: 'dist'
};

export default config;
